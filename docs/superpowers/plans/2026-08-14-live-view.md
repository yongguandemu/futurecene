# 直播间集成主控制界面实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** dashboard 侧边栏新增"直播间"视图（iframe 嵌入 /live2d/），并实现智能助手对话驱动的直播准备意图（加载模型/表情/动作/准备直播）。

**Architecture:** dashboard 增加 `view-live` + nav-item（走既有 hash 路由）；intent_parser 增加 4 条直播意图；CommandRouter 编排 `live2d:prepare`；live2d_stream 前端订阅 `session:switched` 切换角色模型。联动靠 WS 事件广播，无 postMessage。

**Tech Stack:** Flask + EventBus（后端）、原生 JS（前端）。

**Spec:** `docs/superpowers/specs/2026-08-14-live-view-design.md`

---

## 文件结构

```
src/commander/intent_parser.py       修改：新增 4 条直播意图（load/expression/motion/prepare）
src/commander/command_router.py      修改：live2d:prepare 编排（load + 字幕确认）
frontend/dashboard/index.html        修改：新增"直播间"nav-item + view-live iframe
frontend/live2d_stream/index.html    修改：订阅 session:switched 切换角色模型
tests/test_intent_parser.py          修改：新增直播意图断言
tests/test_command_router.py         修改：新增 live2d:prepare 编排断言
```

---

### Task 1: intent_parser 直播意图

**Files:**
- Modify: `src/commander/intent_parser.py`
- Test: `tests/test_intent_parser.py`

- [ ] **Step 1: 写失败测试**（追加 `tests/test_intent_parser.py`）

```python
def test_live2d_load_intent():
    parser = IntentParser()
    cmd = parser.parse("加载小恶魔模型", source="command", session_id="s1")
    assert cmd.capability == "live2d:load"
    assert cmd.payload.get("model_name") == "小恶魔"


def test_live2d_expression_intent():
    parser = IntentParser()
    cmd = parser.parse("做个开心的表情", source="command", session_id="s1")
    assert cmd.capability == "live2d:expression"
    assert cmd.payload.get("expression") == "开心"


def test_live2d_motion_intent():
    parser = IntentParser()
    cmd = parser.parse("挥挥手", source="command", session_id="s1")
    assert cmd.capability == "live2d:motion"
    assert cmd.payload.get("motion") == "wave"


def test_live2d_prepare_intent():
    parser = IntentParser()
    cmd = parser.parse("准备直播界面", source="command", session_id="s1")
    assert cmd.capability == "live2d:prepare"
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_intent_parser.py -v`
Expected: FAIL（默认落到 llm:chat）

- [ ] **Step 3: 实现意图**

`src/commander/intent_parser.py` 增加正则与分支（放在 `_STATUS_RE` 之后、`system:command` 之前）：

```python
# 直播准备意图（直播间集成 · 智能助手联动）
_LIVE2D_LOAD_RE = re.compile(r"^(?:加载|切换)?(?:模型)?\s*(hiyori|小恶魔|yuki|lilith)$",
                             re.IGNORECASE)
_LIVE2D_EXPRESSION_RE = re.compile(r"^做(?:个)?(开心|难过|惊讶|害羞|生气|平静)(?:的表情)?$")
_LIVE2D_MOTION_RE = re.compile(r"^(挥挥手|挥手|点头|摇头|打招呼)$")
_LIVE2D_PREPARE_RE = re.compile(r"^准备(?:一下)?(?:直播|开播)(?:界面)?$")
```

分支（放 `_STATUS_RE` 之后）：

```python
        m = _LIVE2D_LOAD_RE.match(raw)
        if m:
            name = m.group(1).lower()
            return Command(capability="live2d:load",
                           payload={"model_name": "小恶魔" if name in ("小恶魔", "lilith")
                                    else "Hiyori"},
                           source=source, session_id=session_id, raw=raw)

        m = _LIVE2D_EXPRESSION_RE.match(raw)
        if m:
            return Command(capability="live2d:expression",
                           payload={"expression": m.group(1)},
                           source=source, session_id=session_id, raw=raw)

        m = _LIVE2D_MOTION_RE.match(raw)
        if m:
            mapping = {"挥挥手": "wave", "挥手": "wave", "点头": "nod",
                       "摇头": "shake", "打招呼": "wave"}
            return Command(capability="live2d:motion",
                           payload={"motion": mapping[m.group(1)]},
                           source=source, session_id=session_id, raw=raw)

        m = _LIVE2D_PREPARE_RE.match(raw)
        if m:
            return Command(capability="live2d:prepare", payload={},
                           source=source, session_id=session_id, raw=raw)
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/test_intent_parser.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/commander/intent_parser.py tests/test_intent_parser.py
git commit -m "feat(intent): 直播准备意图（模型/表情/动作/准备直播）"
```

---

### Task 2: command_router live2d:prepare 编排

**Files:**
- Modify: `src/commander/command_router.py`
- Test: `tests/test_command_router.py`

- [ ] **Step 1: 写失败测试**（追加 `tests/test_command_router.py`）

```python
def test_live2d_prepare_orchestrates():
    """live2d:prepare → 编排 load（当前角色）+ 字幕确认（直播间集成联动）。"""
    router, bus, orch, _ = _make_router()
    subtitles = []
    bus.subscribe(FRONTEND_SUBTITLE_UPDATE,
                  lambda event, **kw: subtitles.append(kw.get("text", "")))
    result = asyncio.run(router.dispatch(Command(
        capability="live2d:prepare", payload={},
        source="command", session_id="s1")))
    assert result["ok"] is True
    assert any("就绪" in s for s in subtitles)
```

（`_make_router` 现有 FakeOrchestrator 需支持 live2d:load 能力，见 Step 3。）

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_command_router.py::test_live2d_prepare_orchestrates -v`
Expected: FAIL（unknown capability）

- [ ] **Step 3: 实现编排**

`command_router.py` 的 `dispatch` 中，`_inject_llm_context` 之后、`COMMAND_RECEIVED` 发布之前插入：

```python
        # 直播准备编排（直播间集成）：load 当前角色模型 → 字幕确认
        if command.capability == "live2d:prepare":
            return await self._prepare_live(command)
```

新增方法：

```python
    async def _prepare_live(self, command: Command) -> Dict[str, Any]:
        """编排直播准备：live2d:load（当前角色）→ FRONTEND_SUBTITLE_UPDATE 确认。"""
        cid = command.command_id
        role = getattr(self._session, "role", "yuki") if self._session else "yuki"
        orch = self._registry.match("live2d:load")
        load_ok = False
        if orch is not None and self._switch_manager.is_enabled(orch.name):
            try:
                res = await orch.handle({
                    "capability": "live2d:load",
                    "payload": {"role": role, "model_name": command.payload.get("model_name", "") or None},
                })
                load_ok = bool(res.get("ok"))
            except Exception as e:
                logger.warning("[CommandRouter] live2d:prepare load 失败: %s", e)
        model_name = ("小恶魔" if role == "lilith" else "Hiyori")
        text = ("直播界面已就绪，模型 %s 已装载，可以开播啦～" % model_name) if load_ok \
               else "直播界面已就绪（模型由前端加载），可以开播啦～"
        self._event_bus.publish(FRONTEND_SUBTITLE_UPDATE, text=text, role=role,
                                source="live2d:prepare")
        return {"ok": True, "command_id": cid,
                "data": {"prepared": True, "role": role, "model": model_name}}
```

import 增加 `FRONTEND_SUBTITLE_UPDATE`。测试 `_make_router` 的 FakeOrchestrator 若只支持 llm:chat，需扩展或直接跳过 load（load_ok=False 分支仍返回 ok + 字幕）——断言只要求"就绪"字幕，两种分支均满足。

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/test_command_router.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/commander/command_router.py tests/test_command_router.py
git commit -m "feat(router): live2d:prepare 编排（load + 字幕确认）"
```

---

### Task 3: dashboard 直播间视图

**Files:**
- Modify: `frontend/dashboard/index.html`

- [ ] **Step 1: 新增 nav-item**（"智能助手" nav-item 之后）

```html
      <div class="nav-section">直播</div>
      <div class="nav-item" data-page="live">
        <span class="nav-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="6" width="20" height="12" rx="2"/><path d="M10 9l5 3-5 3V9z"/></svg></span>
        <span class="nav-label">直播间</span>
      </div>
```

- [ ] **Step 2: 新增 view-live**（`view-assistant` section 之后）

```html
    <!-- 直播间（iframe 嵌入 live2d_stream，OBS 独立页面保留） -->
    <section class="view" id="view-live">
      <div style="height: calc(100vh - var(--topbar-height) - 44px);">
        <iframe src="/live2d/" width="100%" height="100%" style="border:none;border-radius:var(--radius-card);background:#0b0e14;" title="直播间"></iframe>
      </div>
    </section>
```

- [ ] **Step 3: PAGE_TITLES 增加 live**

```javascript
  var PAGE_TITLES = { assistant: "智能助手", live: "直播间", master: "系统总览", orchestrators: "调度官管理", events: "事件流", schedule: "日程设置", config: "配置管理" };
```

- [ ] **Step 4: node 语法检查**（提取 script 块）

Run: `node --check`
Expected: 语法通过

- [ ] **Step 5: 提交**

```bash
git add frontend/dashboard/index.html
git commit -m "feat(dashboard): 直播间视图（iframe 嵌入 /live2d/）"
```

---

### Task 4: live2d_stream 前端角色联动

**Files:**
- Modify: `frontend/live2d_stream/index.html`

- [ ] **Step 1: 新增 switchActor(role)**

在 `boot()` 附近新增（复用 ROLES 配置与加载逻辑）：

```javascript
  function loadActor(cfg) {
    var url = "/assets/live2d/" + cfg.model + "/" + cfg.model + ".model3.json";
    return PIXI.live2d.Live2DModel.from(url).then(function (model) {
      model.anchor.set(0.5, 1.0);
      model.scale.set(cfg.scale || 0.42);
      model.x = app.screen.width * (cfg.x !== undefined ? cfg.x : 0.5);
      model.y = app.screen.height;
      app.stage.addChild(model);
      actors[cfg.name] = { model: model, state: "silent", timer: null, lastAudio: null };
      setState(cfg.name, "silent");
      return model;
    });
  }
  function switchActor(role) {
    if (!app || !window.PIXI) return;
    if (actors[role]) { setState(role, "silent"); return; }
    var cfg = ROLES.find(function (r) { return r.name === role; });
    if (!cfg) return;
    loadActor(cfg).catch(function () { showHint("模型未就绪：" + cfg.model); });
  }
```

- [ ] **Step 2: boot() 改用 loadActor**

`boot()` 内循环替换为 `await loadActor(cfg)`（try/catch 保留 hint）。

- [ ] **Step 3: 订阅 session:switched**

`wireStore()` 内追加：

```javascript
    store.subscribe("session:", function (state, event) {
      if (event.type === "session:switched" && event.role) {
        switchActor(event.role);
      }
    });
```

- [ ] **Step 4: node 语法检查**

Run: `node --check`
Expected: 语法通过

- [ ] **Step 5: 提交**

```bash
git add frontend/live2d_stream/index.html
git commit -m "feat(live2d_stream): 订阅 session:switched 切换角色模型（联动）"
```

---

### Task 5: 全量回归 + 端到端验收

**Files:**
- 无新增

- [ ] **Step 1: 后端全量回归**

Run: `python -m pytest -q`
Expected: 全部通过

- [ ] **Step 2: 启动系统手动验收**

Run: `python src/app.py`：
- `http://localhost:5000/dashboard/#live` → 直播间 iframe 显示
- 对话"切换到 Lilith" → iframe 模型切换为小恶魔
- 对话"做个开心的表情" → iframe 表情变化
- 对话"准备直播界面" → iframe 字幕"直播界面已就绪"
- `http://localhost:5000/live2d/` 独立访问正常

- [ ] **Step 3: 提交最终验证（如有改动）**

```bash
git add -A
git commit -m "chore(verify): 直播间集成端到端验收"  # 仅验证无改动则跳过
```

---

# 验收清单（最终）

1. dashboard 侧边栏"直播间"项，点击嵌入 live2d 页面正常。
2. 对话"切换到 Lilith"→ iframe 内模型切换。
3. 对话"做个开心的表情"→ iframe 表情变化。
4. 对话"准备直播界面"→ 字幕确认 + 模型装载。
5. `/live2d/` 独立访问仍正常。
6. 全量 pytest 通过。
