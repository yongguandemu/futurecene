# Future Scene 新架构实施规格书 v1.1

> 版本：v1.1
> 日期：2026-08-13
> 架构模式：指挥官-调度官（总 brain - 分 brain）
> 设计原则：模块傻、开关集中、唤醒式调用、事件驱动通信
> 本文档为 P0-P3 全部阶段实施的唯一权威依据，与旧项目无关的部分如有冲突，以本文档为准。

**v1.1 变更记录**：

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-08-13 | 初版：架构总览、通信规范、指挥官/调度官/共享层设计、两级扩展、目录结构、关键链路、测试策略、P0-P3 计划、资产迁移、9 条 ADR |
| v1.1 | 2026-08-13 | 新增第 14 章《模块内容清单（8 项契约）》；P0 拆分为可独立验证的子里程碑；新增 P0 最简仪表盘；密钥管理落地规则；测试压缩为最小集；补充重构背景（角色名统一、接线统一、模块标准化） |

---

## 目录

- [1. 概述](#1-概述)
- [2. 架构总览](#2-架构总览)
- [3. 通信规范](#3-通信规范)
- [4. 指挥官层设计](#4-指挥官层设计)
- [5. 调度官层设计](#5-调度官层设计)
- [6. 共享层设计](#6-共享层设计)
- [7. 两级扩展机制](#7-两级扩展机制)
- [8. 项目目录结构](#8-项目目录结构)
- [9. 关键链路设计](#9-关键链路设计)
- [10. 测试策略](#10-测试策略)
- [11. 开发计划（P0-P3）](#11-开发计划p0-p3)
- [12. 资产迁移计划](#12-资产迁移计划)
- [13. 决策记录（ADR）](#13-决策记录adr)
- [14. 模块内容清单（8项契约）](#14-模块内容清单8项契约)

---

## 1. 概述

### 1.1 背景

旧项目（LumiProject，约 200 个模块）因增量式堆叠形成了三个 2000+ 行巨型文件（`web_server_flask.py` 3655 行、`bootstrapper.py` 2546 行、`lumi_brain.py` 2234 行），事件字符串散落、功能开关分布各处、模块自启动自轮询，导致加功能要改多处、删功能不敢删、定位 bug 需要全链路日志。新项目从零重写，架构纪律从第一天起生效。

重构还解决三个额外问题：

- **角色名统一**：旧项目角色经历 Yuki → Lumi → 双名共存（实际一直使用 Yuki），导致配置、世界书（470 条条目）与角色定义之间出现紊乱。新项目以 `config/profiles/` 为角色唯一来源，世界书按 `metadata.role` 字段严格区分，杜绝双名并存。
- **接线统一**：旧系统各类桥接（memory_bridge、pool_bridge、dialogue_tts_bridge 等）各写各的接线逻辑，风格不一且互相阻塞。新项目由第 3 章双通道通信规范统一接线技术逻辑，所有调度官接线方式一致。
- **模块标准化**：旧项目 100 多个模块风格迥异（部分有 start/stop、部分没有；配置隐式依赖；返回格式不统一）。新项目每个模块必须满足第 14 章《模块内容清单（8 项契约）》，做成可复用的"标准件"。

### 1.2 架构模式

系统采用**指挥官-调度官**两层职责分离：

- **指挥官（Commander，唯一）**：负责任务分发与状态管理，不执行业务逻辑。
- **调度官（Orchestrator，每领域一个）**：只负责一个明确的领域，通过统一的契约协议被指挥官调用。

调度官同时是**领域容器**（分 brain）：每个调度官是一个独立目录，内部模块可独立扩展，互不干扰。

### 1.3 六大设计纪律（必须遵守）

| # | 纪律 | 内容 | 违反后果 |
|---|------|------|----------|
| D1 | 指挥官唯一 | 系统中只有一个指挥官，负责分发和状态管理 | 出现多个决策中心，职责混乱 |
| D2 | 调度官独立 | 每个调度官只负责一个领域，不与其他调度官直接通信 | 耦合度回升，退化为旧系统 |
| D3 | 模块傻 | 所有模块被动工作：不轮询、不监听外部、不维护跨领域状态；被调用时工作，完成后立即返回 | 模块自启动，资源失控 |
| D4 | 开关集中 | 所有功能开关统一在指挥官层管理，调用前必须先检查开关 | 开关散落，无法统一管控 |
| D5 | 事件驱动通知 | 模块间不直接调用，异步通知通过 EventBus 完成 | 隐式依赖重现 |
| D6 | 注册表驱动扩展 | 新功能 = 新建目录 + 注册一次，不修改任何已有模块 | 回归巨型文件堆代码模式 |

### 1.4 术语表

| 术语 | 定义 |
|------|------|
| 指挥官 / Commander / 总 brain | 系统唯一决策与分发入口（`src/commander/`） |
| 调度官 / Orchestrator / 分 brain | 单个业务领域的容器与执行者（`src/orchestrators/`） |
| 能力声明 / capability | 调度官对外声明可处理的能力名，如 `"tts:synthesize"` |
| 命令调用 | 同步调用：指挥官直接调用调度官接口，不走 EventBus |
| 事件通知 | 异步通知：调度官通过 EventBus 发布/订阅 |
| 会话状态 | 跨领域状态（角色、场景、模式），归指挥官管理 |
| 领域内部状态 | 本领域内状态（连接、队列、模型实例），归调度官管理 |

---

## 2. 架构总览

### 2.1 分层架构

```
┌──────────────────────────────────────────────────────────────────────┐
│                          前端层（Frontend）                          │
│   总控台仪表盘 │ Live2D直播源 │ 字幕叠加层 │ 配置面板 │ 桌宠          │
└───────────────────────────────┬──────────────────────────────────────┘
                                │ WebSocket / HTTP（经 API 网关）
┌───────────────────────────────▼──────────────────────────────────────┐
│                     API 网关层（Web Service）                        │
│      Flask/FastAPI 骨架 │ REST 端点 │ WS 端点 │ 静态资源服务         │
└───────────┬───────────────────────────────┬──────────────────────────┘
            │ 命令入口（HTTP/WS 归一化为 command）                        │
┌───────────▼───────────────────────────────▼──────────────────────────┐
│                    指挥官层（Commander Layer）                       │
│  • Intent Parser    自然语言/平台事件 → 结构化指令                    │
│  • Command Router   指令 → 调度官（注册表驱动，不写死映射）          │
│  • Session Context  会话状态维护（角色/场景/模式）                   │
│  • Switch Manager   统一功能开关（注册即自动生成开关项）             │
│  • Orchestrator Registry  分 brain 注册表（唯一注册点）              │
└───────────┬───────────────────────────────┬──────────────────────────┘
            │ 命令调用（同步 await，不走 EventBus）
            │ 事件通知（异步 publish/subscribe，经 EventBus）
┌───────────▼───────────────────────────────▼──────────────────────────┐
│                    调度官层（Orchestrator Layer）                    │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌──────────────────┐  │
│  │ LLM 调度官 │ │ TTS 调度官 │ │ Live2D 调度官│ │ B站 调度官        │  │
│  └────────────┘ └────────────┘ └────────────┘ └──────────────────┘  │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌──────────────────┐  │
│  │屏幕控制调度官│ │记忆管理调度官│ │安全过滤调度官│ │游戏实况调度官      │  │
│  └────────────┘ └────────────┘ └────────────┘ └──────────────────┘  │
│   （每个调度官 = 独立目录容器，内部模块可扩展）                        │
└───────────────────────────────────┬──────────────────────────────────┘
                                    │
┌───────────────────────────────────▼──────────────────────────────────┐
│                    共享层（Shared Layer）                            │
│  • EventBus（通信基座）  • ConfigLoader（配置加载+热重载）            │
│  • Logger（统一日志）    • DataStore（键值存储）                      │
│  • events.py（Event Schema Registry，所有事件名唯一来源）             │
└──────────────────────────────────────────────────────────────────────┘
```

### 2.2 各层职责与边界

| 层 | 允许做什么 | 禁止做什么 |
|----|-----------|-----------|
| 前端层 | 展示、收集输入、通过 API 网关与后端通信 | 直接调用调度官 |
| API 网关层 | 归一化 HTTP/WS 请求为 command 对象、推送事件给前端、服务静态资源 | 执行业务逻辑；除 API 网关外，其他模块禁止常驻监听 |
| 指挥官层 | 意图解析、路由分发、会话状态、开关管理 | 不调用 LLM、不合成语音、不驱动 Live2D（不执行业务逻辑） |
| 调度官层 | 执行本领域业务逻辑、管理领域内部状态 | 不与其他调度官直接通信、不主动轮询、不监听外部 |
| 共享层 | 提供通信/配置/存储基础能力 | 不包含业务逻辑 |

**例外说明**：API 网关层是整个系统中唯一允许"常驻监听"的模块——它本身就是被外部（浏览器/客户端）唤醒的入口，与 D3 模块傻原则不冲突。

---

## 3. 通信规范

### 3.1 双通道通信模型

系统区分两种通信模式，这是旧项目"一切走事件总线导致调用链不透明"问题的根治方案：

| 通道 | 用途 | 实现 | 何时使用 |
|------|------|------|----------|
| **命令调用（同步）** | 指挥官 → 调度官 | 直接 await 调度官公开接口（Python 方法调用） | 需要结果的请求：合成语音、生成回复、加载模型 |
| **事件通知（异步）** | 调度官 → 其他方（指挥官/前端/同领域） | EventBus `publish` / `subscribe` | 不需要结果的告知：任务完成、状态变更、弹幕到达 |

规则：

- 命令调用**不走** EventBus；事件通知**不走**直接 import。
- 调度官对外暴露两类接口：`handle()`（统一入口，供指挥官调用）和**主动事件发布**（通过注入的 EventBus 实例）。
- 任何模块不允许 import 其他调度官的业务模块（如 `from tts_orchestrator import dashscope_tts` 属于违规）。

### 3.2 事件命名规范

- 格式：`{domain}:{action}`，全小写，如 `tts:completed`。
- `requested` 后缀表示请求发出；过去时（`completed`/`failed`/`received`）表示已完成的通知。
- 所有事件名**只在** `src/shared/events.py` 中定义一次，代码中禁止直接书写事件字符串。

### 3.3 Event Schema Registry（events.py 完整清单）

`src/shared/events.py` 是事件名的唯一来源，v1.0 版本完整定义如下：

```python
"""events.py — Event Schema Registry（v1.0）

所有事件名唯一来源。新增事件必须在此定义，禁止在业务代码中手写字符串。
命名：{domain}:{action}，全小写。
"""

# ========== 指挥官域 ==========
COMMAND_RECEIVED = "commander:command_received"      # 指挥官收到新指令
COMMAND_ROUTED = "commander:command_routed"           # 指令已路由到调度官
COMMAND_COMPLETED = "commander:command_completed"     # 指令执行完成
COMMAND_FAILED = "commander:command_failed"           # 指令执行失败

# ========== 会话域 ==========
SESSION_CREATED = "session:created"                   # 会话创建
SESSION_SWITCHED = "session:switched"                 # 角色/场景切换
SESSION_STATE_CHANGED = "session:state_changed"       # 会话状态变更

# ========== 开关域 ==========
SWITCH_CHANGED = "switch:changed"                     # 开关状态变化

# ========== LLM 域 ==========
LLM_REQUESTED = "llm:requested"                       # LLM 调用请求
LLM_RESPONDED = "llm:responded"                       # LLM 响应完成（含完整文本）
LLM_STREAM_CHUNK = "llm:stream_chunk"                 # LLM 流式分片
LLM_FAILED = "llm:failed"                             # LLM 调用失败

# ========== TTS 域 ==========
TTS_REQUESTED = "tts:requested"                       # TTS 合成请求
TTS_COMPLETED = "tts:completed"                       # TTS 合成完成
TTS_FAILED = "tts:failed"                             # TTS 合成失败
TTS_AUDIO_READY = "tts:audio_ready"                   # 音频就绪（payload 含音频 ID/路径）

# ========== Live2D 域 ==========
LIVE2D_LOADED = "live2d:loaded"                       # 模型加载完成
LIVE2D_EXPRESSION_CHANGED = "live2d:expression_changed"  # 表情切换
LIVE2D_MOTION_TRIGGERED = "live2d:motion_triggered"   # 动作触发
LIVE2D_LIP_SYNC_START = "live2d:lip_sync_start"       # 口型同步开始
LIVE2D_LIP_SYNC_END = "live2d:lip_sync_end"           # 口型同步结束

# ========== B站平台域 ==========
BILIBILI_CONNECTED = "bilibili:connected"             # B站连接建立
BILIBILI_DISCONNECTED = "bilibili:disconnected"       # B站连接断开

# ========== 归一化观众域（多平台统一） ==========
DANMAKU_RECEIVED = "danmaku:received"                 # 弹幕（归一化后）
GIFT_RECEIVED = "gift:received"                       # 礼物（归一化后）
GUARD_RECEIVED = "guard:received"                     # 上舰（归一化后）
SUPERCHAT_RECEIVED = "superchat:received"             # SuperChat（归一化后）
AUDIENCE_ENTERED = "audience:entered"                 # 进场（归一化后）
AUDIENCE_FILTERED = "audience:filtered"               # 被安全过滤拦截

# ========== 记忆域 ==========
MEMORY_STORED = "memory:stored"                       # 记忆已写入
MEMORY_RETRIEVED = "memory:retrieved"                 # 记忆已检索
MEMORY_CONSOLIDATED = "memory:consolidated"           # 短期→长期固化完成

# ========== 安全域 ==========
SAFETY_BLOCKED = "safety:blocked"                     # 内容被拦截
SAFETY_FLAGGED = "safety:flagged"                     # 内容被标记（需人工确认）

# ========== 游戏实况域 ==========
GAME_VN_STATE_CHANGED = "game:vn_state_changed"       # VN 画面状态变化
GAME_MC_STATE_CHANGED = "game:mc_state_changed"       # MC 状态变化
GAME_COMMENTARY_REQUESTED = "game:commentary_requested"  # 请求生成解说

# ========== 前端域 ==========
FRONTEND_STATUS_UPDATE = "frontend:status_update"     # 系统状态推送（供总控台）
FRONTEND_SUBTITLE_UPDATE = "frontend:subtitle_update" # 字幕更新（供字幕叠加层）

# ========== 音频域 ==========
AUDIO_SEGMENT_READY = "audio:segment_ready"           # 音频分片就绪（供播放/口型）
```

### 3.4 领域内协作例外规则

D2"调度官之间不直接通信"允许一个例外：**同领域内**的调度官可以通过 EventBus 事件直接协作，但禁止 import 对方模块。事件中只传轻量引用（音频 ID、模型路径、状态值），禁止传大对象（原始音频字节、图片数据）。

当前定义的领域分组：

| 领域 | 成员调度官 | 允许的事件协作示例 |
|------|-----------|-------------------|
| 表达领域 | TTS、Live2D | `tts:audio_ready` → Live2D 口型同步（只传音频 ID） |
| 观众领域 | B站、安全 | `danmaku:received` → 安全过滤（只传文本） |

跨领域的协作（如 TTS → 游戏实况）必须经指挥官协调，不走调度官直接事件订阅。

### 3.5 大对象传递规则

- 音频、图片、视频等二进制数据**不进入事件负载**。
- 统一模式：写入共享的临时/缓存目录（如 `data/cache/`），事件中传文件 ID 或相对路径。
- 由消费方（如 Live2D 口型同步、前端播放器）按 ID 自行读取。

---

## 4. 指挥官层设计

### 4.1 职责边界

指挥官只做四件事：

1. **分发**：接收归一化指令，路由到对应调度官，汇总结果。
2. **状态管理**：维护会话状态（角色、场景、模式）与开关状态。
3. **注册管理**：维护分 brain 注册表（新增/查询/启停）。
4. **编排**：跨领域流程的组合调用（声明式步骤，不写业务逻辑）。

指挥官**不执行**：LLM 调用、语音合成、Live2D 驱动、弹幕收发等一切业务动作。

### 4.2 分 brain 注册表（orchestrator_registry.py）

```python
# src/commander/orchestrator_registry.py
"""分 brain 注册表 — 系统唯一注册点。

原则：注册即生效。注册时自动完成三件事：
1. 将分 brain 加入可路由列表（供 Command Router 动态查询）
2. 自动创建对应开关项（供 Switch Manager 查询/控制）
3. 按声明顺序调用 start() 启动（依赖声明顺序）
"""

class OrchestratorRegistry:
    def __init__(self, switch_manager, event_bus):
        self._orchestrators: dict[str, OrchestratorProtocol] = {}
        self._switch_manager = switch_manager
        self._event_bus = event_bus

    def register(self, orchestrator) -> None:
        """注册分 brain。注册即生成开关、即可路由。"""
        if orchestrator.name in self._orchestrators:
            raise DuplicateOrchestrator(orchestrator.name)
        self._orchestrators[orchestrator.name] = orchestrator
        self._switch_manager.auto_register(orchestrator.name)   # 自动生成开关项
        orchestrator.start()                                     # 启动（注入 EventBus 后）

    def unregister(self, name: str) -> None:
        self._orchestrators[name].stop()
        del self._orchestrators[name]
        self._switch_manager.auto_unregister(name)

    def get(self, name: str) -> "OrchestratorProtocol | None":
        return self._orchestrators.get(name)

    def all(self) -> list["OrchestratorProtocol"]:
        return list(self._orchestrators.values())

    def match(self, capability: str) -> "OrchestratorProtocol | None":
        """按能力名匹配调度官（Command Router 使用）。"""
        for orch in self._orchestrators.values():
            if capability in orch.capabilities():
                return orch
        return None
```

### 4.3 调度官契约协议（protocols.py）

所有分 brain 必须实现 `OrchestratorProtocol`，这是扩展机制成立的前提：

```python
# src/commander/protocols.py
from typing import Protocol, Any, Dict, List

class OrchestratorProtocol(Protocol):
    """所有分 brain 必须实现的契约。"""

    name: str                                    # 分 brain 唯一标识，如 "tts"

    def capabilities(self) -> List[str]:
        """能力声明，如 ["tts:synthesize", "tts:stop"]。
        总 brain 只认能力名，不关心实现细节。"""

    async def handle(self, command: Dict[str, Any]) -> Dict[str, Any]:
        """统一入口：command = {"capability": str, "payload": dict}
        返回 {"ok": bool, "data": dict, "error": str | None}"""

    async def start(self) -> None:
        """注册时由 Commander 调用；按注册顺序执行。"""

    async def stop(self) -> None:
        """注销/关闭时调用；逆序执行。"""

    def health(self) -> Dict[str, Any]:
        """健康检查，返回 {"status": "ok"|"degraded"|"down", "detail": str}"""
```

模块级处理器契约（分 brain 内部模块）另行约定，见 5.2。

### 4.4 Intent Parser（输入边界）

Intent Parser 只处理**需要角色回应的输入**。直播场景下输入分两类，处理路径不同：

| 输入类型 | 来源 | 处理路径 |
|----------|------|----------|
| 用户意图 | 弹幕文本、私信、语音指令、前端命令 | Intent Parser：文本 → 结构化命令 |
| 平台事件 | 礼物、上舰、SuperChat、进场、关注 | 事件归一化（各平台适配器内完成）→ 直接发 `gift:received` 等事件，不走 Intent Parser |

Intent Parser 输出统一的结构化命令对象：

```python
# src/commander/intent_parser.py
@dataclass
class Command:
    capability: str            # 能力名，如 "llm:chat"
    payload: dict              # 结构化参数，如 {"text": "...", "role": "yuki"}
    source: str                # 来源：danmaku / command / voice / system
    session_id: str            # 会话 ID
    raw: str                   # 原始文本（供审计/调试）
```

v1.0 的意图分类（规则驱动，后续可升级模型驱动）：

| 意图 | 触发示例 | 路由能力 |
|------|----------|----------|
| 闲聊对话 | 普通弹幕 | `llm:chat` |
| 点歌 | `!点歌 xxx` / 含歌名关键词 | `music:request`（P2 后） |
| 感谢 | 含"谢谢"等 | `llm:chat`（人设驱动） |
| 切换角色 | `!切换 yuki/lilith` | `session:switch`（指挥官内部处理） |
| 系统命令 | `!状态` / `!暂停` | 指挥官内部 / `switch:set` |
| 未知意图 | 无法归类 | 默认路由 `llm:chat` |

### 4.5 Command Router（注册表驱动，不写死映射）

```python
# src/commander/command_router.py
class CommandRouter:
    """注册表驱动路由：遍历注册表按能力匹配，无静态映射表。

    加分 brain 无需修改本文件 — 注册即路由。"""

    def __init__(self, registry: OrchestratorRegistry, switch_manager, event_bus):
        self._registry = registry
        self._switch_manager = switch_manager
        self._event_bus = event_bus

    async def dispatch(self, command: Command) -> Dict[str, Any]:
        self._event_bus.publish(COMMAND_RECEIVED, command=command)

        # D4 纪律：调用前必须检查开关
        orch = self._registry.match(command.capability)
        if orch is None:
            return {"ok": False, "error": f"unknown capability: {command.capability}"}
        if not self._switch_manager.is_enabled(orch.name):
            return {"ok": False, "error": f"orchestrator disabled: {orch.name}"}

        self._event_bus.publish(COMMAND_ROUTED, capability=command.capability, target=orch.name)
        try:
            result = await orch.handle({"capability": command.capability, "payload": command.payload})
            self._event_bus.publish(COMMAND_COMPLETED, capability=command.capability, result=result)
            return result
        except Exception as e:
            self._event_bus.publish(COMMAND_FAILED, capability=command.capability, error=str(e))
            return {"ok": False, "error": str(e)}
```

### 4.6 Session Context（状态边界）

状态归属规则（ADR-001）：

| 状态类型 | 示例 | 归属 | 实现 |
|----------|------|------|------|
| 会话状态 | 当前角色、场景、直播模式、日程 | 指挥官 | `SessionContext` |
| 领域内部状态 | WebSocket 连接、音频队列、当前模型实例、表情状态机 | 调度官 | 各调度官内部，暴露 `reset()`/`health()` |

```python
# src/commander/session_context.py
@dataclass
class SessionContext:
    session_id: str
    role: str = "yuki"              # 当前角色
    scene: str = "chat"             # 当前场景：chat / game / vn / mc
    live_mode: str = "offline"      # offline / live
    started_at: float = field(default_factory=time.time)

    def switch_role(self, role: str) -> None:
        self.role = role
        # 发布事件供前端/调度官感知
```

### 4.7 Switch Manager（开关集中 + 自动生成）

```python
# src/commander/switch_manager.py
class SwitchManager:
    """统一开关管理。

    - 开关项由分 brain 注册自动生成（见 OrchestratorRegistry.register）
    - 手动覆盖优先级最高：手动禁用后，任何启用请求均被拒绝
    - 支持日程驱动 + 手动覆盖（延续旧项目 feature_switch 验证过的设计）
    """

    def __init__(self, event_bus):
        self._event_bus = event_bus
        self._defaults: dict[str, bool] = {}    # 默认状态（注册时设置）
        self._manual: dict[str, bool] = {}      # 手动覆盖（优先级最高）

    def auto_register(self, name: str, default: bool = True) -> None:
        self._defaults[name] = default

    def is_enabled(self, name: str) -> bool:
        if name in self._manual:
            return self._manual[name]
        return self._defaults.get(name, True)

    def set_manual(self, name: str, enabled: bool) -> None:
        self._manual[name] = enabled
        self._event_bus.publish(SWITCH_CHANGED, name=name, enabled=enabled)

    def clear_manual(self, name: str) -> None:
        self._manual.pop(name, None)
        self._event_bus.publish(SWITCH_CHANGED, name=name, enabled=self.is_enabled(name))

    def snapshot(self) -> dict[str, bool]:
        return {k: self.is_enabled(k) for k in self._defaults}
```

---

## 5. 调度官层设计

### 5.1 分 brain 规范（目录即容器）

每个调度官是一个独立目录，满足以下规范：

- 目录名 = `{domain}_orchestrator/`，如 `tts_orchestrator/`。
- 必须包含 `__init__.py`（导出主类）和 `registry.py`（内部模块注册表）。
- 主类必须实现 `OrchestratorProtocol`（4.3）。
- 分 brain 内部模块**可以**直接 import 同目录的其他模块（这是唯一允许的直接 import）。
- 分 brain 之间禁止 import。

### 5.2 内部模块注册表（registry.py）

分 brain 内部注册表支持两类条目（ADR：模块级扩展两类结构）：

| 条目类型 | 含义 | 示例 |
|----------|------|------|
| 单实现能力 | 该能力只有一个处理器 | `"tts:stop": StopHandler` |
| 多实现能力 | 同一能力多个引擎，按配置选择 | `"tts:synthesize": [DashScopeHandler, EdgeTTSHandler]` |

```python
# tts_orchestrator/registry.py 示例
HANDLERS = {
    "tts:synthesize": [DashScopeHandler, EdgeTTSHandler],  # 引擎候选，按配置选
    "tts:stop": [StopHandler],
    "tts:cache_clean": [CacheCleanHandler],
}

def resolve(capability: str, engine: str | None = None) -> Handler:
    """多实现按配置的 engine 名选择，单实现直接返回。"""
    handlers = HANDLERS[capability]
    if len(handlers) == 1:
        return handlers[0]()
    for h in handlers:
        if h.engine_name == engine:
            return h()
    raise UnknownEngine(capability, engine)
```

### 5.3 生命周期协议

| 阶段 | 调用方 | 时机 | 行为 |
|------|--------|------|------|
| `start()` | Commander | 注册时（按注册顺序） | 建立内部资源（连接、加载配置），**不启动任何业务行为** |
| `handle()` | Commander | 每次指令 | 执行本领域逻辑，返回结果，**无内部循环/轮询** |
| `health()` | Commander/看门狗 | 定时/按需 | 报告状态，供总控台与自愈使用 |
| `stop()` | Commander | 注销/关闭时（逆序） | 释放资源，保存领域内部状态（如有必要） |

### 5.4 各分 brain 接口定义（v1.0）

#### LLM 调度官（llm_orchestrator）

| 能力名 | 输入 payload | 输出 data | 说明 |
|--------|--------------|-----------|------|
| `llm:chat` | `{text, role, scene, history_ids?, memory_ids?}` | `{reply, usage, latency_ms}` | 对话生成 |
| `llm:stream_chat` | 同上 | 通过 `llm:stream_chunk` 事件流式输出 | 流式对话 |
| `llm:classify` | `{text, labels}` | `{label, confidence}` | 意图/情感分类 |
| `llm:health` | 无 | `{status}` | 健康检查 |

**职责边界（ADR-005）**：LLM 调度官只做 LLM API 调用封装（流式、重试、降级、成本统计）。**不包含**：

- 角色人格组装（system prompt）→ 由指挥官 Session Context 按角色配置生成，注入 `llm:chat` 的 `payload["system_prompt"]`
- 记忆注入 → 指挥官在调用前向记忆调度官检索，把结果放入 payload
- 意图路由 → 归 Intent Parser

#### TTS 调度官（tts_orchestrator）

| 能力名 | 输入 payload | 输出 data | 说明 |
|--------|--------------|-----------|------|
| `tts:synthesize` | `{text, role, voice?, engine?}` | `{audio_id, duration_ms}` | 合成音频（写缓存目录，返回 ID） |
| `tts:stream_synthesize` | `{text, role}` | 通过 `tts:audio_ready` 分片发布 | 流式合成（长文本分片） |
| `tts:stop` | 无 | `{stopped: true}` | 停止当前合成/播放队列 |
| `tts:cache_clean` | `{max_age_hours?}` | `{cleaned, freed_bytes}` | 清理音频缓存 |

引擎切换：`tts:synthesize` 的 `engine` 参数选择实现（dashscope/edge 等），由内部 registry 的 `resolve()` 完成，对指挥官透明。

#### Live2D 调度官（live2d_orchestrator）

| 能力名 | 输入 payload | 输出 data | 说明 |
|--------|--------------|-----------|------|
| `live2d:load` | `{model_name}` | `{loaded: true}` | 加载模型（默认小恶魔） |
| `live2d:expression` | `{expression}` | `{applied: true}` | 切换表情 |
| `live2d:motion` | `{motion}` | `{triggered: true}` | 触发动作 |
| `live2d:lip_sync` | `{audio_id}` | `{started: true}` | 按音频口型同步（监听 `tts:audio_ready` 自动触发） |
| `live2d:param` | `{params: dict}` | `{applied: true}` | 注入任意参数（视线/呼吸等） |

领域内部状态：当前模型实例、表情状态机、参数插值器状态。

#### B站调度官（bilibili_orchestrator）

| 能力名 | 输入 payload | 输出 data | 说明 |
|--------|--------------|-----------|------|
| `bilibili:connect` | `{room_id?}` | `{connected: true}` | 建立直播连接（WebSocket） |
| `bilibili:disconnect` | 无 | `{disconnected: true}` | 断开连接 |
| `bilibili:send_message` | `{text}` | `{sent: true}` | 发送弹幕消息 |
| `bilibili:get_stream_code` | 无 | `{rtmp_url, stream_key}` | 获取推流码 |

归一化职责：B站事件（弹幕/礼物/上舰/SuperChat/进场）在适配器内部归一化为 `AudienceEvent`，发布 `danmaku:received` 等统一事件。B站签名逻辑（HMAC-SHA256）从旧项目 `external_apps/bilibili_adapter.py` 直接复制。

#### 屏幕控制调度官（screen_control_orchestrator）

| 能力名 | 输入 payload | 输出 data | 说明 |
|--------|--------------|-----------|------|
| `screen:capture` | `{region?, window_title?}` | `{image_path}` | 截图（mss + PrintWindow 支持 D3D 窗口） |
| `screen:click` | `{x, y, button?}` | `{done: true}` | 鼠标点击（SendInput） |
| `screen:keypress` | `{key, repeat?}` | `{done: true}` | 键盘输入 |
| `screen:describe` | `{image_path?}` | `{description}` | 画面理解（OCR/视觉模型） |
| `screen:execute_plan` | `{plan: list}` | `{results}` | 按操作计划序列执行（点击/拖动/按键） |

#### 记忆调度官（memory_orchestrator）

| 能力名 | 输入 payload | 输出 data | 说明 |
|--------|--------------|-----------|------|
| `memory:store` | `{content, role?, tags?, ttl?}` | `{memory_id}` | 写入短期记忆 |
| `memory:retrieve` | `{query, k?, role?}` | `{memories: [...]}` | 检索相关记忆（关键词/向量） |
| `memory:consolidate` | 无 | `{consolidated: n}` | 短期→长期固化 |
| `memory:get_history` | `{session_id, limit?}` | `{history: [...]}` | 获取对话历史 |

持久化：v1.0 用本地 JSON/SQLite（旧 `data_store.py` + `permanent_memory_store.py` 模式），后续可升级向量库。

#### 安全调度官（safety_orchestrator）

> ADR-014：`safety:check_input` / `safety:check_output` 已退役（信任厂商安全系统，
> 角色边界由世界书人设唯一性维持）。模块与 `safety:reload_rules` 能力保留注册。

| 能力名 | 输入 payload | 输出 data | 说明 |
|--------|--------------|-----------|------|
| ~~`safety:check_input`~~ | ~~`{text, source?}`~~ | ~~`{verdict: allow/block/flag, reason?}`~~ | ~~输入侧过滤（ADR-014 退役）~~ |
| ~~`safety:check_output`~~ | ~~`{text}`~~ | ~~`{verdict: allow/block/flag, reason?}`~~ | ~~输出侧过滤（ADR-014 退役）~~ |
| `safety:reload_rules` | 无 | `{loaded: n}` | 热重载敏感词规则 |

安全模型（`data/safety_model/`）从旧项目迁移，v1.0 以关键词规则为主，模型推理为增强项。

#### 游戏实况调度官（game_orchestrator）

| 能力名 | 输入 payload | 输出 data | 说明 |
|--------|--------------|-----------|------|
| `game:vn_start` | `{profile_name}` | `{started: true}` | 启动 VN 陪看会话 |
| `game:vn_stop` | 无 | `{stopped: true}` | 停止 VN 陪看 |
| `game:vn_state` | 无 | `{state: 对白/选项/菜单/...}` | 当前画面状态（轮询由本调度官内部定时驱动，属领域内部行为） |
| `game:mc_start` | `{mode?}` | `{started: true}` | 启动 MC 实况 |
| `game:mc_stop` | 无 | `{stopped: true}` | 停止 MC 实况 |
| `game:commentary` | `{scene_state}` | `{commentary_text}` | 生成解说文本 |

**合并说明（ADR）**：旧项目的三套 VN 实现（`visual_novel/`、`vn_adapter.py`、`vn_bridge.py`）在 P3 合并为一个统一模块，功能对比矩阵见 11.4。

### 5.5 分 brain 职责边界清单（哪些不做）

| 调度官 | 禁止行为 |
|--------|----------|
| LLM | 不做人格组装、不做意图判断、不做记忆注入 |
| TTS | 不驱动 Live2D（只发布 `tts:audio_ready`，由表达领域订阅） |
| Live2D | 不做语音合成 |
| B站 | 不做弹幕内容处理（只归一化发布，消费由指挥官编排） |
| 屏幕控制 | 不主动截屏（除游戏实况调度官内部定时外） |
| 记忆 | 不主动固化（由指挥官按会话节奏触发 `memory:consolidate`） |
| 安全 | 不决定回复策略（只返回 verdict，策略归指挥官） |
| 游戏实况 | 不直接调用 LLM 生成解说（发布 `game:commentary_requested`，经指挥官编排） |

### 5.6 决策分级与上抛机制（决策归属矩阵）

任何决策在落地前先回答「该由谁决定」。四级决策模型，越往下决策越少、成本越高、越接近总脑：

| 层 | 名称 | 决策者 | 特征 | 例子 |
|----|------|--------|------|------|
| L0 | 反射 | 过滤器/规则（无人） | 有硬规则可判，零 LLM 零咨询 | 脏话弹幕 keyword_filter 拦截；@点名仲裁 mention 规则 |
| L1 | 域内自治 | 各调度官 | 只影响本域、可逆、低成本 | 插火把（learn_brain）、切歌、截图 |
| L2 | 仲裁上抛 | 仲裁器（确定性规则链） | 跨域冲突或资源互斥 | 谁有发言权（arbitrator） |
| L3 | 总脑编排 | 指挥官 | 需全局上下文或高风险不可逆 | 回复弹幕（danmaku_pipeline）、开播/下播、切换角色 |

#### 上抛触发逻辑（三问框架）

`src/shared/decision_policy.classify()` 显式实现，顺序判定：

1. 有硬规则命中？→ L0（拦截/放行）
2. 决策归属矩阵显式声明？→ 按矩阵层
3. 只影响本域且可逆且低成本？→ L1 域内自决
4. 需要全局上下文 / 高风险 / 不可逆？→ L3 总脑
5. 跨域冲突？→ L2 仲裁
6. 兜底 → L1

#### 决策归属矩阵

矩阵是上抛判定的唯一显式来源，代码实现于 `src/shared/decision_policy.DECISION_MATRIX`（支持 `*` 通配前缀，精确键优先）。新增能力必须在此登记归属层；未登记的能力走三问推断，保证每个调度官开发时都知道自己的决策边界。

| 能力（前缀） | 层 | 处置 | 依据 |
|--------------|----|------|------|
| `safety:reload_rules` | L0 | execute | 规则热加载属安全域内部（`safety:check_input`/`check_output` 已按 ADR-014 退役） |
| `screen:capture` / `screen:click` / `screen:keypress` / `screen:*` | L1 | execute | 截图/点击/按键域内操作 |
| `music:*` | L1 | execute | 播放控制/点歌域内自治（切歌） |
| `experience:*` | L1 | execute | 经验决策域内闭环（插火把） |
| `bilibili:connect` / `disconnect` / `send_message` / `get_stream_code` | L1 | execute | 平台连接与消息执行通道 |
| `adapter:obs_connect/scene/screenshot/source`、`adapter:qq_*`、`adapter:vts_*` | L1 | execute | 平台适配域内操作 |
| `stream:alive` / `fetch_code` / `launch_app` / `app_*` | L1 | execute | 推流状态/应用进程域内操作 |
| `collab:*` | L2 | escalate | 发言权/协作仲裁（确定性规则链） |
| `game:vn_start` / `vn_stop` / `mc_start` / `mc_stop` | L2 | escalate | 启停游戏实况涉屏幕/推流/解说多域 |
| `llm:chat`（及 `llm:*`） | L3 | escalate | 回复弹幕需全局上下文 |
| `game:commentary` | L3 | escalate | 解说生成需全局情境上下文 |
| `adapter:obs_stream` | L3 | escalate | 推流启停即开播/下播，高风险不可逆 |
| `stream:start` / `stream:stop` | L3 | escalate | 开播/下播高风险不可逆 |
| `session:switch` | L3 | escalate | 切换角色/会话需全局上下文 |

#### 不动作作为合法决策（决策日志）

仲裁返回 None、规则判不了静默等行为从隐含改为显式：所有决策点写入 `src/shared/decision_log`，每次记录发布 `decision:logged` 事件。outcome 六值：

| outcome | 含义 |
|---------|------|
| executed | 执行了动作 |
| blocked | 被硬规则拦截（L0） |
| no_action | 收到但决定不动作，reason_code 说明「为何不回应」 |
| deferred | 决策已作出但排队等待（发言互斥） |
| escalated | 上抛到仲裁/总脑 |
| failed | 决策过程失败 |

调试判别法：系统没收到 → 无日志条目；系统决定不回应 → 一条 `outcome=no_action` 且 `reason_code` 明确的条目。查看 `GET /api/decisions` 或订阅 `decision:logged` 即可区分。

已接线决策点：arbitrator（`arbitrate_no_winner` / `arbitrated` / `speech_queue_deferred`）、learn_brain（`no_candidate_action` / `fuse_paused` / `post_action_cooldown` / `empty_goal` / `push_failed`）、danmaku_pipeline（`safety_input_blocked` / `safety_output_blocked` / `llm_empty_reply` / `llm_not_injected` / `system_command_escalated`）、command_router（`unknown_capability` / `orchestrator_disabled`）。周期心跳类日志按 (source, reason_code) 限频，防刷屏。

---

## 6. 共享层设计

### 6.1 EventBus

从旧项目 `core/event_bus.py`（284 行）直接复制，做两处微调：

1. 增加事件名校验：`publish()`/`subscribe()` 时校验事件名存在于 `events.py`（防止字符串散落）。
2. 增加同步发布选项：`publish_sync()` 用于需要等待所有订阅者完成的场景（默认仍异步）。

```python
# src/shared/event_bus.py（要点）
class EventBus:
    def __init__(self, schema: set[str]):
        self._schema = schema          # 来自 events.py 的事件名集合
        self._subscribers: dict[str, list[Callable]] = {}
        self._history: deque = deque(maxlen=1000)   # 事件历史（调试用）

    def subscribe(self, event: str, handler: Callable, priority: int = 50) -> None:
        self._validate(event)
        ...

    def publish(self, event: str, **kwargs) -> None:
        self._validate(event)
        ...

    def publish_sync(self, event: str, **kwargs) -> None:
        self._validate(event)
        for handler in sorted(self._subscribers.get(event, []), key=lambda h: -h.priority):
            handler(event=event, **kwargs)
```

### 6.2 ConfigLoader

- 加载 `config/` 目录下全部 YAML/JSON/ENV 文件。
- 支持热重载：`watch()` 监听文件变更，变更后发布 `config:changed` 事件（在 events.py 中注册）。
- 提供分层覆盖：`defaults < env < config/*.yaml`。
- 密钥统一从环境变量读取（`.env`），配置文件不写明文密钥（旧项目教训）。

**密钥管理落地规则（P0 生效）**：

1. P0 首日建立 `.env.example` 模板，列出全部需要的环境变量名（含用途说明）。v1.1 已知清单：`OPENAI_API_KEY`、`ZHIPU_API_KEY`、`DASHSCOPE_API_KEY`、`WUSOUND_API_KEY`、`BILIBILI_ACCESS_KEY_ID`、`BILIBILI_ACCESS_KEY_SECRET`、`BILIBILI_COOKIE`、`OBS_WS_PASSWORD`。
2. ConfigLoader 在启动时校验全部必填环境变量：缺失时打印清晰错误（`[config] missing required env: OPENAI_API_KEY`）并**退出**，禁止用空字符串继续运行。
3. 迁移旧配置时**禁止直接复制** `config.yaml` 等含密钥文件；先将敏感值替换为 `${{ env.XXX }}` 占位符，再写入新项目 `config/`，运行时由 ConfigLoader 从环境变量注入。
4. 迁移后立即在旧项目侧核对，确保没有其他明文密钥残留在新项目任何文件中（P0 完成标志之一）。

### 6.3 Logger

- 统一日志，支持按模块名过滤（`logger.get("tts")` 只输出 tts 域日志）。
- 日志级别可运行时调整（API 网关提供 `/api/system/log_level` 端点，延续旧项目设计）。
- 结构化输出：时间、级别、模块、事件名（如有）。

### 6.4 DataStore

- 键值持久化（JSON 文件/SQLite），供记忆调度官、会话恢复、成本统计使用。
- 接口：`get(key)` / `set(key, value)` / `delete(key)` / `snapshot()`。

### 6.5 API 网关（Web Service）

归共享层，`src/web/` 目录。这是系统对外唯一入口，也是唯一常驻监听模块。

v1.0 端点清单：

| 类型 | 端点 | 说明 |
|------|------|------|
| REST | `GET /api/health` | 健康检查（含各调度官 health() 汇总） |
| REST | `GET /api/state` | 系统状态快照（供总控台轮询） |
| REST | `GET /api/config` | 配置查询（脱敏，不返回密钥） |
| REST | `POST /api/command` | 命令入口（前端发指令 → Commander） |
| REST | `POST /api/switch/{name}` | 开关控制（启/停调度官） |
| WS | `/ws/events` | 事件推送（前端订阅事件流，如状态更新/字幕） |
| WS | `/ws/tts_audio` | 音频流推送（TTS → 浏览器播放，延续旧项目已验证设计） |
| 静态 | `/` | 前端资源服务（dashboard 等） |

代码结构强制使用 Blueprint 拆分，禁止单体文件（旧项目 3655 行单文件教训）。

---

## 7. 两级扩展机制

### 7.1 领域级扩展（加分 brain）

新增一个业务领域的完整流程，**不修改任何已有模块**：

```text
1. 在 src/orchestrators/ 下新建目录 xxx_orchestrator/
2. 实现 __init__.py：导出主类（实现 OrchestratorProtocol）
3. 实现 registry.py：声明本分 brain 能处理的能力（capabilities 从 registry 派生）
4. 在指挥官启动装配处（commander/app.py 的装配列表）追加一行注册
5. 在 shared/events.py 中定义新事件名（如有）
6. 完成 — 无需改 bootstrapper、无需改 web 服务器、无需改其他分 brain
```

`capabilities()` 直接从内部 registry 的能力键派生，避免两处维护：

```python
# xxx_orchestrator/__init__.py
class XxxOrchestrator:
    name = "xxx"

    def capabilities(self) -> List[str]:
        return list(registry.HANDLERS.keys())   # 派生，不手写
```

### 7.2 模块级扩展（补模块）

在现有分 brain 下补充能力的流程，**不影响其他分 brain，也不影响总 brain**：

```text
1. 在该分 brain 目录下新建模块文件（如 edge_tts.py）
2. 在 registry.py 中注册（单实现或多实现条目）
3. 完成 — 总 brain 无需任何改动，其他分 brain 无需任何改动
```

多实现切换示例：`tts:synthesize` 配置 `engine: "edge"` 时，`resolve()` 自动选择 EdgeTTSHandler，`dashscope_tts.py` 不受影响。

### 7.3 扩展机制与旧系统对比

| 操作 | 旧系统 | 新系统 |
|------|--------|--------|
| 新增功能领域 | 改 bootstrapper + web_server + brain if-else | 新建目录 + 装配列表加一行 |
| 替换模块实现 | 巨型文件翻找多处引用 | 目录内新增文件 + registry 一条记录 |
| 关闭功能 | 开关散落多处 | Switch Manager 一处生效（开关自动生成） |
| 定位 bug | 事件谁发谁订不可查 | events.py + capability 声明可查 |
| 新增事件 | 手写字符串，可能漏改订阅者 | events.py 定义 + EventBus 运行时校验 |

---

## 8. 项目目录结构

### 8.1 完整目录树

```text
D:\project build\
├── src/
│   ├── __init__.py
│   ├── app.py                    # 装配入口：注册分 brain、启动 API 网关
│   ├── commander/                # 指挥官层（总 brain）
│   │   ├── __init__.py
│   │   ├── orchestrator_registry.py
│   │   ├── protocols.py
│   │   ├── intent_parser.py
│   │   ├── command_router.py
│   │   ├── session_context.py
│   │   └── switch_manager.py
│   ├── orchestrators/            # 调度官层（分 brain 容器）
│   │   ├── __init__.py
│   │   ├── llm_orchestrator/
│   │   │   ├── __init__.py
│   │   │   ├── registry.py
│   │   │   ├── openai_client.py
│   │   │   └── glm_client.py
│   │   ├── tts_orchestrator/
│   │   │   ├── __init__.py
│   │   │   ├── registry.py
│   │   │   ├── dashscope_tts.py
│   │   │   └── edge_tts.py
│   │   ├── live2d_orchestrator/
│   │   │   ├── __init__.py
│   │   │   ├── registry.py
│   │   │   └── webgl_bridge.py
│   │   ├── bilibili_orchestrator/
│   │   │   ├── __init__.py
│   │   │   ├── registry.py
│   │   │   ├── connector.py      # WS 长连接（签名逻辑直接复制旧项目）
│   │   │   └── normalizer.py     # 平台事件 → AudienceEvent
│   │   ├── screen_control_orchestrator/
│   │   │   ├── __init__.py
│   │   │   ├── registry.py
│   │   │   ├── capture.py
│   │   │   ├── input.py
│   │   │   └── vision.py
│   │   ├── memory_orchestrator/
│   │   │   ├── __init__.py
│   │   │   ├── registry.py
│   │   │   ├── short_term.py
│   │   │   ├── long_term.py
│   │   │   └── retriever.py
│   │   ├── safety_orchestrator/
│   │   │   ├── __init__.py
│   │   │   ├── registry.py
│   │   │   ├── keyword_filter.py
│   │   │   └── model_filter.py   # 安全模型推理（增强）
│   │   └── game_orchestrator/
│   │       ├── __init__.py
│   │       ├── registry.py
│   │       ├── vn_session.py     # 三套 VN 实现合并后
│   │       └── mc_bridge.py
│   ├── shared/                   # 共享层
│   │   ├── __init__.py
│   │   ├── events.py             # Event Schema Registry（唯一事件名来源）
│   │   ├── event_bus.py
│   │   ├── config_loader.py
│   │   ├── logger.py
│   │   └── data_store.py
│   └── web/                      # API 网关层
│       ├── __init__.py
│       ├── app_factory.py        # Blueprint 组装
│       ├── routes/
│       │   ├── health.py
│       │   ├── state.py
│       │   ├── command.py
│       │   ├── switch.py
│       │   └── ws.py
│       └── static/               # 前端静态资源
├── assets/                       # 从旧项目迁移
│   ├── live2d/小恶魔/            # 主模型（P0）
│   ├── live2d/Haru/              # 示例模型（可选）
│   ├── live2d/Hiyori/            # 示例模型（可选）
│   ├── audio/                    # 参考音频（yuki_voice_reference.wav 等）
│   ├── images/
│   └── streaming/                # 直播间 UI 图片
├── config/                       # 配置文件
│   ├── profiles/                 # Yuki/Lilith 角色配置
│   ├── visual_novel_profiles/    # VN 作品配置
│   ├── config.yaml               # 主配置（不含密钥）
│   ├── tts_config.yaml
│   ├── live2d_config.yaml
│   ├── live_ai_config.yaml
│   ├── danmaku_config.yaml
│   └── safety_config.yaml
├── data/
│   ├── worldbook.json            # 世界书（470条）
│   ├── safety_model/             # 安全模型（model.pt 等）
│   ├── memory/                   # 记忆持久化
│   ├── cache/                    # 音频/图片缓存（事件大对象中转区）
│   └── topic/                    # 话题列表
├── frontend/                     # 前端（P1 起组件化）
│   ├── dashboard/                # 总控台
│   ├── live2d_stream/            # Live2D 直播源
│   ├── subtitle_overlay/         # 字幕叠加层
│   └── vendor/                   # 第三方库（pixi/live2d/echarts）
├── tests/                        # 测试（P0 建立骨架）
│   ├── test_events_schema.py
│   ├── test_event_bus.py
│   ├── test_command_router.py
│   └── test_switch_manager.py
├── deploy/                       # 部署（Docker/CI-CD，P2 后）
├── docs/                         # 文档（本规格书等）
├── .env.example                  # 环境变量模板（密钥只进环境变量）
├── requirements.txt
└── pyproject.toml
```

### 8.2 目录职责说明

- `src/app.py`：装配入口，唯一的"启动主函数"（对应旧项目 bootstrapper 的角色，但只做装配，不做业务）。
- `src/web/`：API 网关，唯一常驻监听模块。
- `data/cache/`：事件大对象中转区（音频 ID 指向的文件所在处）。
- `tests/`：与 src 同构（`tests/test_commander/`、`tests/test_orchestrators/`）。

---

## 9. 关键链路设计

### 9.1 完整调用链路（用户输入 → 前端更新）

```text
用户输入（弹幕/命令/语音）
    │
    ▼
API 网关 /ws/events 或 /api/command      [归一化：HTTP/WS → Command 对象]
    │
    ▼
Intent Parser                             [文本 → 结构化 Command]
    │
    ▼
Command Router                            [查注册表匹配调度官]
    │
    ├─ 检查 Switch Manager（D4 纪律）      [禁用则返回错误]
    │
    ▼
对应调度官 handle()                       [同步命令调用，不走 EventBus]
    │
    ├─ 内部 registry 解析处理器
    ├─ 执行领域逻辑
    └─ 发布完成事件（如 llm:responded）
    │
    ▼
指挥官汇总结果 → 编排后续步骤（如需 TTS/Live2D）
    │
    ▼
TTS 调度官 handle() → 发布 tts:audio_ready（含音频 ID）
    │
    ├─ 前端订阅 tts_audio WS → 浏览器播放
    └─ Live2D 订阅（表达领域协作）→ 口型同步
    │
    ▼
发布 frontend:status_update / frontend:subtitle_update
    │
    ▼
前端更新（总控台/字幕层/Live2D）
```

### 9.2 弹幕 → 对话 → TTS → Live2D 链路（P2 验收链路）

```text
B站调度官（WS 收到弹幕）
  → normalizer 归一化 → publish danmaku:received
      → 指挥官订阅：组装 Command{capability:"llm:chat", payload:{text, role}}
      → 记忆调度官 handle(memory:retrieve)     ← 指挥官编排调用
      → LLM 调度官 handle(llm:chat)
          → 指挥官收到回复（内容安全信任厂商，ADR-014 不再本地过滤）
      → TTS 调度官 handle(tts:synthesize)
          → publish tts:audio_ready {audio_id}
      → 前端 WS 播放 + Live2D 口型同步（表达领域订阅）
```

链路中**所有调度官调用都由指挥官发起**（命令调用），调度官之间只存在"表达领域"的事件协作。

### 9.3 错误处理与降级链路

| 故障场景 | 处理方式 |
|----------|----------|
| LLM 调用失败 | LLM 调度官降级链：primary → fallback（如 openai → glm）→ 本地兜底回复；发布 `llm:failed` |
| TTS 合成失败 | 发布 `tts:failed`，指挥官决定降级为字幕显示（无语音播报） |
| B站连接断开 | 调度官发布 `bilibili:disconnected`，自动重连（指数退避），指挥官记录会话降级 |
| 调度官 handle 异常 | Router 捕获 → 发布 `commander:command_failed` → 返回结构化错误给前端 |
| 成本超限 | 成本熔断器（P3）拦截 LLM/TTS 调用，返回提示性回复 |
| 开关被手动关闭 | Router 在调用前拦截，返回 `orchestrator disabled`，前端展示开关状态 |

---

## 10. 测试策略

### 10.1 测试层级

| 层级 | 覆盖对象 | 工具 |
|------|----------|------|
| 单元测试 | EventBus、ConfigLoader、Switch Manager、registry、各处理器 | pytest |
| 契约测试 | 每个分 brain 实现 OrchestratorProtocol 的行为（mock 外部 API） | pytest + pytest-asyncio |
| 集成测试 | 完整链路（假弹幕 → LLM mock → TTS mock → 事件断言） | pytest + 事件历史断言 |
| 冒烟测试 | P0 基座启动 + /api/health 返回全部调度官 ok | pytest + httpx |

### 10.2 P0 基座测试清单

**第一次提交必须通过的最小测试集（M0 门槛）**——收到规格书后应立即开始编码，先把这三组测试跑绿，再补其他：

| 测试文件 | 断言内容 | 必写理由 |
|----------|----------|----------|
| `test_event_bus.py` | 发布/订阅/优先级/历史/未知事件名抛错 | EventBus 是全部通信基座，其余模块都依赖它 |
| `test_config_loader.py` | 环境变量缺失时报错退出、占位符注入正确 | 密钥落地规则的正确性验证 |
| `test_web_health.py` | 服务启动后 `/api/health` 返回 ok | 最小可运行的最终判据 |

M0 之外的分阶段测试（随子里程碑补充，见 11.1 各里程碑验收）：

| 测试文件 | 断言内容 | 所属里程碑 |
|----------|----------|-----------|
| `test_events_schema.py` | 事件名唯一、格式符合 `{domain}:{action}` | M1 |
| `test_switch_manager.py` | 自动注册生成开关、手动覆盖优先 | M2 |
| `test_orchestrator_registry.py` | 注册/注销/重复注册抛错/能力匹配 | M2 |
| `test_command_router.py` | 未知能力返回错误、开关禁用拦截、路由成功 | M3 |
| `test_session_context.py` | 角色切换、状态变更事件发布 | M3 |
| `test_dashboard_api.py` | `/api/state` 返回状态快照、事件流推送 | M4 |

### 10.3 测试纪律

- 新分 brain 注册时必须附带最少一个契约测试。
- 核心链路（弹幕→LLM→TTS→Live2D）必须有集成测试（旧项目核心链路裸奔的教训）。
- 测试不得依赖真实外部 API（LLM/TTS/B站全部 mock），外部 API 单独用冒烟测试验证。
- 测试按"先跑最小集、后补其余"的顺序推进：M0 三组测试是全项目继续开发的前置门槛，未过 M0 不得开始 P1。

---

## 11. 开发计划（P0-P3）

### 11.1 P0 — 基座（1-2 周）

**执行原则**：P0 拆分为 6 个可独立验证的子里程碑（M0-M5），**逐个完成、逐个验收，不一次性做完再验证**。目标是"尽快让系统跑起来"，不是"完成规格书里所有内容再跑"。每个里程碑的验收通过后才进入下一个；允许 M1-M5 有部分延后项，但 M0 是硬门槛（见 10.2）。

| 里程碑 | 交付物 | 验收标准（独立可验证） |
|--------|--------|----------------------|
| **M0 最小可运行** | 目录骨架、`shared/event_bus.py`（复制旧项目+微调）、`shared/config_loader.py`（含环境变量校验）、`shared/logger.py`、`.env.example`、`src/web/app_factory.py`（最小 Blueprint）+ `/api/health` | `python src/app.py` 能启动；`/api/health` 返回 ok；`test_event_bus.py` + `test_config_loader.py` + `test_web_health.py` 三组测试全绿 |
| **M1 事件基座** | `shared/events.py`（3.3 全部事件名 + schema 校验集成进 EventBus）、`test_events_schema.py` | EventBus 拒绝未注册事件名；`test_events_schema` 通过 |
| **M2 指挥官骨架** | `shared/data_store.py`、`commander/switch_manager.py`、`commander/orchestrator_registry.py`、`commander/protocols.py` | 注册/注销/能力匹配/开关自动生成单测全绿（`test_switch_manager`、`test_orchestrator_registry`） |
| **M3 路由与装配** | `commander/command_router.py`、`commander/session_context.py`、`commander/intent_parser.py`（最小规则集）、`src/app.py` 装配 | 路由单测全绿（未知能力/开关拦截/成功/异常）；`/api/health` 返回注册表快照 |
| **M4 最简仪表盘** | `frontend/dashboard/index.html`（单页：系统状态 + 事件流 + 开关列表，无设计系统要求）+ `/api/state` + `/ws/events` | 浏览器打开页面能看到：系统已启动、注册了哪些分 brain、事件流实时滚动、开关可点击切换 |
| **M5 资产迁移** | 12.1 全部 P0 资产迁移 + 配置占位符化（6.2 规则） | 迁移清单逐项勾销；`config/` 中无明文密钥；启动日志无密钥残留 |

**M4 最简仪表盘说明**：P0 阶段没有完整前端设计系统，最简仪表盘只承担"可观察性"职责——用原生 HTML+JS 单页展示 `/api/state` 快照和 `/ws/events` 事件流，让分 brain 注册、事件路由、开关切换的验证从"看终端日志"升级为"看页面"，缩短每次修改后的验证周期。完整总控台（设计令牌、双主题）仍是 P1 交付物。

**P0 完成标志（全部里程碑通过后）**：`python src/app.py` 启动后浏览器可访问最简仪表盘，看到系统状态与事件流；`pytest` 全绿；无明文密钥在代码/配置中。

### 11.2 P1 — 核心功能（3-4 周）

**交付物**：

| 项 | 内容 | 验收标准 |
|----|------|----------|
| LLM 调度官 | openai_client + glm_client（降级链） | mock 单测：chat 成功/降级/失败 |
| TTS 调度官 | dashscope_tts + 流式分片 + 缓存目录 | 真实合成一次语音成功 |
| Live2D 调度官 | webgl_bridge + 模型加载 + 表情 + 口型 | 浏览器加载小恶魔模型成功 |
| 指挥官完整装配 | Intent Parser + Router + 三个分 brain | 完整链路测试通过 |
| 前端总控台 | 仪表盘读取 `/api/state` + 开关控制 | 浏览器操作开关实时生效 |
| 集成测试 | 输入→LLM(mock)→TTS(mock)→Live2D(mock) | 事件历史断言全绿 |

**P1 完成标志**：前端输入一句话，后端完成"LLM 回复 → TTS 合成 → Live2D 口型"全链路，总控台可看到状态流转。

### 11.3 P2 — 平台集成（2-3 周）

**交付物**：

| 项 | 内容 | 验收标准 |
|----|------|----------|
| B站调度官 | connector（签名逻辑复制旧项目）+ normalizer | 真实连接直播间收到弹幕 |
| 直播互动管线 | danmaku:received → 指挥官编排 → 对话 | 弹幕触发角色回复（9.2 链路） |
| 前端字幕叠加层 + Live2D 直播源 | OBS 浏览器源 | OBS 添加源后可显示 |
| Switch Manager 日程驱动 | 按日程自动启停功能 | 定时任务场景测试通过 |

**P2 完成标志**：真实直播间弹幕 → 角色语音回复 → Live2D 口型同步 → 字幕显示，全链路无人工干预。

### 11.4 P3 — 特色场景（3-4 周）

**交付物**：

| 项 | 内容 | 验收标准 |
|----|------|----------|
| 屏幕控制调度官 | capture/input/vision | 自动截图+点击测试通过 |
| 游戏实况调度官 | VN 陪看（三套合并）+ MC 实况 | VN 自动推进+解说测试 |
| 记忆调度官 | 短期/长期/检索 | 记忆存取测试全绿 |
| 安全调度官 | 关键词 + 模型推理 | 敏感词拦截测试全绿 |
| 运维保障 | 成本追踪 + 熔断 + 看门狗 | 熔断触发测试通过 |
| 部署 | Docker + CI-CD | 容器内启动通过 |

**VN 三套实现合并的功能对比矩阵**（P3 开工前必须完成逐条确认）：

| 功能 | visual_novel/ | vn_adapter.py | vn_bridge.py | 合并去向 |
|------|--------------|---------------|--------------|----------|
| 会话 tick 循环 | 有 | 3s 检查周期 | 间隔点击推进 | 统一到 vn_session.py |
| 状态识别 | 关键词匹配 | OCR 台词检测 | 截屏识别 | 统一 + 可选视觉模型 |
| 自动推进 | dry_run + pyautogui | SendInput Enter | 间隔点击 | 统一为可配置策略 |
| 解说生成 | 吐槽策略 | 情绪联动 | 上报云端 | 统一到 game:commentary |
| 节奏控制 | 最小间隔 20s | 去重不刷屏 | 无 | 保留两者，合并规则 |

---

## 12. 资产迁移计划

### 12.1 迁移清单（P0 优先项含补充）

| 资产 | 来源（`{{OLD_PROJECT_ROOT}}`） | 目标 | 优先级 | 备注 |
|------|-------------------------------|------|--------|------|
| 小恶魔 Live2D 模型 | `LumiProject\assets\live2d\小恶魔\` | `assets\live2d\小恶魔\` | P0 | 含 vtube.json、7 表情、8192 纹理 |
| 角色配置 Yuki/Lilith | `LumiProject\profiles\yuki\`、`lilith\` | `config\profiles\` | P0 | 每角色 6 文件 |
| 世界书 | `LumiProject\data\worldbook.json` | `data\worldbook.json` | P0 | 470 条 |
| 全局配置 7 个 yaml | `LumiProject\config\*.yaml` | `config\` | P0 | 需剥离密钥 |
| 安全模型 | `LumiProject\data\safety_model\` | `data\safety_model\` | P0 | **补充项**（规格书 1.0 遗漏，已修正） |
| 参考音频 | `LumiProject\assets\voice_samples\yuki_voice_reference.wav` | `assets\audio\` | P0 | **补充项**：TTS 声音克隆必需 |
| 前端第三方库 | `LumiProject\dashboard\vendor\` | `frontend\vendor\` | P0 | **补充项**：Live2D 渲染必需 |
| 直播间 UI 图片 | `LumiProject\assets\streaming\` | `assets\streaming\` | P0 | 13 张 PNG |
| 话题列表 | `LumiProject\topic\` | `data\topic\` | P1 | 中英文话题 |
| VN 作品配置 | `LumiProject\config\visual_novel_profiles\atri.yaml` | `config\visual_novel_profiles\` | P1 | |
| 记忆缓存 | `LumiProject\memory\l1_cache.json`、`l2_cache.json` | `data\memory\` | P3 | 可选，可重新积累 |
| B站签名逻辑 | `LumiProject\core\external_apps\bilibili_adapter.py` | `src\orchestrators\bilibili_orchestrator\connector.py` | P2 | 直接复制 HMAC-SHA256 部分 |
| 屏幕控制 Windows API | `LumiProject\core\screen_control\` | `src\orchestrators\screen_control_orchestrator\` | P3 | 需参数化 tesseract 路径 |
| EventBus 代码 | `LumiProject\core\event_bus.py` | `src\shared\event_bus.py` | P0 | 284 行，复制+微调 |

### 12.2 迁移顺序

1. **P0 首日**：目录骨架 + 全部 P0 资产复制到位（Live2D/配置/世界书/安全模型/参考音频/vendor/图片/yaml）。
2. **P1**：前端页面参考（`dashboard_new.html` 设计令牌提取 CSS 变量）。
3. **P2**：B站签名逻辑、live_ai 管线设计参考。
4. **P3**：屏幕控制、记忆系统代码参考。
5. **不迁移**：三个巨型文件（仅作设计参考）、TTS 缓存、运行时截图、调试页面、Electron 二进制。

---

## 13. 决策记录（ADR）

### ADR-001 状态归属边界

- **状态**：Accepted
- **决策**：会话状态归指挥官（SessionContext），领域内部状态归各调度官（连接/队列/模型实例），调度官暴露 `reset()`/`health()` 接口统一管理。
- **动机**："调度官完全无状态"会导致所有状态上收指挥官，制造新的上帝对象（旧 Bootstrapper 教训）。

### ADR-002 双通道通信模型

- **状态**：Accepted
- **决策**：命令调用走同步 Python 接口（指挥官 → 调度官，await），事件通知走 EventBus（调度官 → 其他方）。命令调用不走 EventBus。
- **动机**：全事件化通信导致调用链不透明、同步语义丢失、调试困难（旧系统核心痛点）。

### ADR-003 领域内协作例外

- **状态**：Accepted
- **决策**：允许"表达领域"（TTS↔Live2D）和"观众领域"（B站↔安全）内调度官通过 EventBus 事件协作，禁止 import 对方模块，事件只传轻量引用。
- **动机**：口型同步等场景经指挥官中转会增加延迟和复杂度；事件只传 ID 保持解耦。

### ADR-004 API 网关归位

- **状态**：Accepted
- **决策**：API 网关（`src/web/`）归属共享层，是唯一常驻监听模块，Blueprint 结构强制拆分。
- **动机**：旧 `web_server_flask.py` 3655 行单文件是核心阻塞项；架构图遗漏 Web 服务器会导致隐式前置依赖。

### ADR-005 LLM 调度官职责边界

- **状态**：Accepted
- **决策**：LLM 调度官只做 API 调用封装；人格组装归 SessionContext、记忆注入由指挥官编排、意图路由归 Intent Parser。
- **动机**：旧 `lumi_brain.py` 2234 行因职责混杂而失控，任何一项越界都会让它重新长成巨型模块。

### ADR-006 注册表驱动路由

- **状态**：Accepted
- **决策**：Command Router 无静态映射表，遍历 OrchestratorRegistry 按 `capabilities()` 能力匹配；`capabilities()` 从内部 registry 派生。
- **动机**：静态路由映射需要加分 brain 时修改 router，违反开闭原则。

### ADR-007 开关项自动生成

- **状态**：Accepted
- **决策**：分 brain 注册时自动生成 Switch Manager 开关项，手动覆盖优先级最高，支持日程驱动。
- **动机**：开关列表手写会导致"加功能改两处"，注册即生效保证单一改动点。

### ADR-008 注册表可见性边界

- **状态**：Accepted
- **决策**：OrchestratorRegistry 只对 Commander 层可见，分 brain 之间禁止通过注册表获取其他分 brain 实例。
- **动机**：注册表若被分 brain 相互查询使用，退化为 Service Locator 反模式，耦合度回升。

### ADR-009 大对象不进事件

- **状态**：Accepted
- **决策**：音频/图片等二进制数据写入 `data/cache/`，事件只传文件 ID/路径，由消费方自行读取。
- **动机**：大对象在事件中传递造成内存压力和序列化开销，且违反事件轻量化原则。

### ADR-010 模块内容清单（8 项契约）

- **状态**：Accepted
- **决策**：每个模块必须满足第 14 章的 8 项内容契约；这是"内容清单"而非"代码格式"，不绑定类/函数组织方式。新增/评审模块时按 8 项检查；AI 生成代码时把 8 项作为指令的一部分。
- **动机**：旧项目 100+ 模块风格迥异、配置隐式依赖、返回格式不统一；统一内容契约不锁死实现灵活性，是模块标准化的最小约束。

### ADR-011 P0 里程碑化与最简仪表盘

- **状态**：Accepted
- **决策**：P0 拆分为 M0-M5 六个可独立验证的子里程碑（10.2 的 M0 三组测试为硬门槛）；M4 交付最简仪表盘（原生单页，仅承担可观察性职责），完整总控台延后到 P1。
- **动机**：完整规格书对单人开发可能成为拖延借口；缺可视化反馈会拉长验证周期（风险 2、3）。

### ADR-012 密钥管理落地规则

- **状态**：Accepted
- **决策**：P0 首日建立 `.env.example`；ConfigLoader 启动时校验必填环境变量，缺失即报错退出（禁止空字符串运行）；旧配置迁移一律占位符化（`${{ env.XXX }}`），禁止直接复制含密钥文件。
- **动机**：规格书"密钥走环境变量"的要求若不落地，迁移时明文密钥会直接暴露在新项目配置中（风险 4）。

### ADR-013 决策分级与上抛机制

- **状态**：Accepted
- **决策**：决策归属显式化——新增 `shared/decision_policy`（四级模型 + 三问框架 + `DECISION_MATRIX` 归属矩阵）与 `shared/decision_log`（`decision:logged` 事件，outcome 含 no_action）。上抛是例外通道而非默认通道；「不动作」是合法决策，必须记录 reason_code。
- **动机**：上抛触发条件此前隐含在仲裁器/规则链里，调度官开发时不知道自己的决策边界；「不动作」无法与「没收到」区分，调试困难（用户反馈三项：触发条件未显式化、L1 边界不清晰、不动作未显式定义）。

### ADR-014 内容安全信任厂商 + 人设唯一性维持角色边界

- **状态**：Accepted
- **决策**：移除弹幕链路中的输入/输出安全过滤环节（`safety:check_input` / `safety:check_output` 退役，不再被 `danmaku_pipeline` 调用，决策矩阵条目删除）。内容安全信任厂商（DeepSeek/智谱）安全系统；角色边界改由世界书「人设唯一性」正向设定维持（`wb_yuki_identity_uniqueness` / `wb_lilith_identity_uniqueness`，character 分类，随 `system_prompt_block` 注入）。`safety` 调度官模块与 `safety:reload_rules` 能力保留注册。
- **动机**：本地关键词/模型过滤会误伤正常输入且增加链路延迟与模型 prompt 负担；厂商大模型安全系统覆盖更全、更新更快，比本地硬规则更可靠。角色不「出戏」是产品核心体验，用正向人设设定（「你就是 Yuki/Lilith 本人」）比否定式安全规则更有效、更省 token。

---

## 14. 模块内容清单（8项契约）

### 14.1 定位与原则

8 项契约是**每个模块必须包含的内容**，缺任何一项模块就不完整。它不是代码结构——不绑定类/函数组织、命名规范、文件布局；而是模块的"内容契约"，保证 100+ 模块风格一致、边界清晰、可评审、可复用。

三项硬性要求：

1. 开发新模块时：先按 8 项填写内容清单，再写代码。
2. 评审模块时：用 8 项作为检查清单。
3. AI 生成代码时：把 8 项作为指令的一部分，要求生成代码同时输出 8 项内容。

### 14.2 八项契约定义

| # | 契约项 | 内容要求 | 作用 |
|---|--------|----------|------|
| 1 | **模块身份标识** | 所属调度官（如 `tts_orchestrator`）；能力名（如 `tts:synthesize`）；引擎名（多实现时填写，单实现可省略） | 让调度官知道模块是谁、能做什么 |
| 2 | **配置契约** | 需要哪些配置项（如 `api_key`、`voice`、`timeout`）；必填/默认值；数据类型与取值范围 | 让调用方知道传入什么配置模块才能工作 |
| 3 | **输入契约** | 接收什么格式的输入（如 `{"text": str, "role": str}`）；必填/可选字段；数据类型与取值范围 | 让调用方知道传入什么参数模块才能工作 |
| 4 | **输出契约** | 返回格式统一为 `{"ok": bool, "data": {...}, "error": str}`；成功/失败时分别返回什么字段 | 让调用方知道如何解析返回结果 |
| 5 | **依赖声明** | 依赖哪些外部服务（如 DashScope API）；依赖哪些内部模块；是否需要预先配置（如 API Key） | 让部署维护者知道模块能否在当前环境运行 |
| 6 | **错误定义** | 可能抛出的错误类型（如 `APIKeyMissingError`、`RateLimitError`）；每种错误的处理建议（重试/降级/熔断） | 让调用方知道如何处理错误 |
| 7 | **生命周期方法** | 需要哪些生命周期操作（`init`/`start`/`stop`/`health`）；哪些必须实现、哪些可选；各阶段做什么 | 让调度官知道如何管理模块完整生命周期 |
| 8 | **领域状态说明** | 维护哪些领域内部状态（如 `_last_request_time`、`_connection`）；是否需要持久化；状态如何恢复（如断线重连） | 让调用方知道模块是否有状态、重启后是否需要恢复 |

### 14.3 完整模板（以 TTS 调度官模块为例）

```markdown
# 模块内容清单 — dashscope_tts

## 1. 模块身份标识
- 所属调度官：tts_orchestrator
- 能力名：tts:synthesize
- 引擎名：dashscope

## 2. 配置契约
| 配置项 | 必填 | 默认值 | 类型/范围 | 说明 |
|--------|------|--------|-----------|------|
| api_key | 是 | 无 | str，非空 | DashScope API Key（环境变量注入） |
| voice | 否 | "longxiaochun" | str | 音色名 |
| timeout | 否 | 30 | int，1-120（秒） | 合成超时 |
| format | 否 | "mp3" | str ∈ {mp3, wav, pcm} | 输出格式 |

## 3. 输入契约
- 输入格式：`{"text": str, "role": str, "engine"?: str}`
- text：必填，str，1-2000 字符
- role：必填，str ∈ {yuki, lilith}，决定 voice 兜底
- engine：可选，str，多实现路由用

## 4. 输出契约
- 成功：`{"ok": true, "data": {"audio_id": str, "duration_ms": int}}`
- 失败：`{"ok": false, "data": {}, "error": str}`

## 5. 依赖声明
- 外部服务：DashScope TTS API（需 api_key）
- 内部模块：本调度官 registry（resolve 多实现）、shared/config_loader
- 预先配置：环境变量 DASHSCOPE_API_KEY 必须存在

## 6. 错误定义
| 错误类型 | 触发条件 | 处理建议 |
|----------|----------|----------|
| APIKeyMissingError | 未配置 api_key | 启动时拦截，禁止运行 |
| RateLimitError | 调用频率超限 | 指数退避重试（最多 3 次） |
| QuotaExceededError | 配额用尽 | 降级到备用引擎（edge），发布 tts:failed |
| TimeoutError | 超时 | 重试 1 次，失败则降级 |

## 7. 生命周期方法
| 方法 | 必须 | 行为 |
|------|------|------|
| init | 是 | 读取配置、校验 api_key |
| start | 是 | 建立客户端连接池 |
| stop | 是 | 关闭连接池 |
| health | 是 | 返回连接状态与最近调用时间 |

## 8. 领域状态说明
- 状态项：`_client`（连接池）、`_last_request_time`（最近调用时间）
- 持久化：无（全部可重建）
- 恢复：start 时重建连接池；断线由 DashScope 客户端自动重连
```

### 14.4 新旧项目 8 项覆盖对比

| 内容项 | 旧项目 | 新项目（目标） |
|--------|--------|----------------|
| 模块身份标识 | 散落各处，无集中定义 | 模块头部集中声明 + registry 注册 |
| 配置契约 | 硬编码或隐式依赖 | config 参数显式注入 + 校验 |
| 输入契约 | 隐式依赖，改参数不改文档 | 头部明确声明 + 验证 |
| 输出契约 | 各模块返回格式不同 | 统一 `{"ok": bool, "data": {...}, "error": str}` |
| 依赖声明 | 无统一位置 | 模块头部或同目录 README 声明 |
| 错误定义 | 类型不统一，处理散落 | 模块内统一定义 + 调用方统一处理 |
| 生命周期方法 | 部分有 start/stop，部分没有 | 统一 init/start/stop/health |
| 领域状态说明 | 无记录，修改后不可追溯 | 模块内说明状态类型与恢复方式 |

### 14.5 与代码结构的关系

8 项契约不绑定代码格式。模块可以在 `__init__.py` 按类组织，也可以在 `functions.py` 按函数组织；只要 8 项内容完整，模块就是完整的。代码结构（类/函数、命名规范、文件组织）是下一层问题，在内容清单稳定后再定，不在本章约束范围内。

### 14.6 模块清单落盘位置

每个模块的 8 项内容以 `模块内容清单` 形式存放于：

- 单文件模块：模块头部的 docstring 中（如 `# 模块内容清单 — dashscope_tts`）。
- 多文件模块：同目录 `MODULE_CONTRACT.md`（按 14.3 模板组织）。
- 8 项内容同时作为该模块在 registry 注册时的校验输入（registry 检查能力名、输入/输出契约格式合法后才允许注册）。

---

> 本规格书 v1.1 为 P0-P3 实施的唯一权威依据。实施过程中如发现规格书与实现冲突，先更新规格书（追加 ADR）再改代码，禁止绕过纪律直接改实现。P0 按 11.1 的 M0-M5 里程碑逐个推进，M0 三组测试为硬门槛。
